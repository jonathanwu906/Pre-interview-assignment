# 量化交易關聯分析

三題各自對應交易系統裡的真實問題。這份文件整理對應關係，細節都以各題的 design doc 與實測為準。

## Q1 權限設計

這題的形狀——巢狀群組攤平成集合、熱路徑只剩一次查找——在交易系統裡到處出現：

| 本題的做法 | 交易系統裡的同一件事 |
|---|---|
| 慢路徑先攤平、熱路徑一次 hash 查找 | Pre-trade 檢查：開盤前攤平權限，下單路徑不做任何遍歷 |
| child → parent 向上收集 | 限額用量往上加總（trader → desk → firm） |
| naive 的向下展開 | Kill switch：停掉一個 desk，底下所有帳戶都要擋到 |
| 查詢變高頻時改建 principal → 檔案的反向索引 | 市場資料 entitlement：訂閱時正查、稽核時反查，兩個方向都要快 |
| ACL 與身分集合取交集 | 可交易 universe 判定（階層分類聯集 + 白名單） |
| ID 只做精確比對（U5n ≠ U5nn） | 商品識別碼：BRK.A／BRK-A、ISIN／CUSIP 混用，寬鬆比對就是下錯商品 |
| 兩份獨立實作互相對答案 | 獨立定價模型對產線模型、新舊撮合引擎的驗收方式 |
| 固定 seed，不用平台會變的隨機源 | 回測可重現：換機器、換版本重跑，結論不能變 |
| 掃到下限之後，再快只能把工作搬到慢路徑先做 | 低延遲系統的通用打法：熱路徑到底了就往開盤前搬 |
| 快取權限要有失效上界 | 撤銷的授權若延遲生效就是合規事故，失效策略跟稽核一起設計 |

幾個對應多說兩句：

**Pre-trade 檢查**。每張訂單送出前要確認這個 trader 能不能交易這個商品（restricted list、商品範圍、venue 權限）。延遲預算以微秒計，熱路徑上不可能遍歷組織圖或打 RPC——做法跟本題一樣，開盤前和權限異動時把身分攤平成 in-memory set，下單時只剩一次查找。Benchmark 那個 22 倍量的就是這件事。

**市場資料 entitlement**。交易所資料按 user／firm／device 計費、會被稽核，違規有真實罰款。訂閱時要正查（這個 user 能不能看這個 feed）、稽核時要反查（這個 feed 有誰在看），兩邊都是巢狀群組——兩個方向都要快，所以兩套索引都得維護。

**限額與 kill switch**。限額用量向上加總，就是本題 child → parent 的方向；kill switch 停掉一個 desk、底下全部要擋，是向下展開的方向。兩個遍歷方向在交易系統各有真實用途。

**可交易 universe**。「這檔標的在不在我的 universe」：universe 是階層分類（sector／industry）的聯集加白名單，結構上就是一條 ACL；判定就是商品的分類集合跟它有沒有交集。

Q1 和 Q2 是同一件事的兩面：Q1 把**查詢**搬離熱路徑（預先攤平），Q2 把**配置**搬離熱路徑（預先付清 kernel 成本）——熱路徑上只留查找和指標操作。

## Q2 myMalloc

低延遲交易系統的慣例是「啟動時把記憶體配好，交易時段的熱路徑不碰 malloc」；這題就是把這個慣例做成元件：

| 本題的做法 | 交易系統裡的同一件事 |
|---|---|
| 啟動時 mmap 並先寫過每一頁，之後不進 kernel | 開盤前 warmup：訂單物件池、訊息 buffer 先配好，盤中禁 malloc |
| alloc/free 是 worst-case O(1)，路徑固定 | tick-to-trade 的延遲預算可以編列，因為每步的成本是常數 |
| 延遲分佈穩定：p99 在 40–130 ns，跨情境跨平台不動 | p99 比平均值重要——一次尖峰就是一次錯過的價格 |
| 一執行緒一個 pool，沒有共享就沒有鎖 | per-core／per-strategy 各自一塊記憶體，熱路徑沒有同步點 |
| `fallback_count` 對外可查 | 池用完是容量規劃出錯的前兆，接進監控當警訊，不當正常降級 |
| 對 heap 歷史免疫（glibc 上髒 heap 讓 malloc 慢 5.6 倍，pool 不動） | 系統跑滿整個交易日，延遲不隨執行歷史漂移 |
| 同尺寸穩態只剩 1.2×，照實寫 | 買的是延遲穩定、不是吞吐——跟上 kernel bypass／FPGA 是同一種理由 |
| LIFO 先發還剛釋放的 slot | 熱資料留在 L1/L2：訊息物件反覆用同一塊還熱的 buffer |

## Q3 色塊矩陣

找規律跟 alpha research 是同一種工作：分辨真訊號和碰巧。

| 本題的做法 | 量化研究裡的同一件事 |
|---|---|
| 每個猜測都拿隨機盤面對照出現機率 | 策略績效要跟隨機基準比，不是看回測曲線順眼 |
| 找到規則後，剩下的疑似規律放回「規則成立的隨機盤面」重測，乍看顯著的欄位相似就消失了 | 市場裡到處是這種假訊號：控制既有因子後，新因子的增量常常歸零 |
| 試了十幾個猜測就申報十幾個 | 掃過上千個訊號之後，5% 顯著水準毫無意義 |
| 拿斜角方向當對照組（跟隨機一樣） | 安慰劑檢定：把訊號放到不該有效的市場或時段，應該失效 |
| 敢說「這裡沒東西」（顏色數量就是隨機波動） | 敢對雜訊說不做——不部署硬掰出來的策略，本身就有價值 |
| 星 2 判讀為干擾項、同時明說可能有漏測的解釋 | 結論要交代把握程度和邊界，不是二元的有效／無效 |
| 先數位化，全流程可以程式重跑 | 研究可重現：從原始資料到結論一鍵重跑，不靠手工步驟 |

三題合起來：Q1、Q2 是執行端（把工作搬離熱路徑、把延遲做成常數），Q3 是研究端（分辨訊號和碰巧）——正好是量化交易的兩側。
